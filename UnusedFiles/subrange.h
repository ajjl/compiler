//<subrange> ::= <expression> ".." <expression>

#ifndef EXTERN 
#define EXTERN extern
#endif

class subrange {
public:
   static subrange * compile( Environment * e );



private:

// nothing lives here yet

};

#undef EXTERN
